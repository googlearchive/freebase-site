var static_url = function(path) { 
  return "http://s.freebase.com" + path;
};

var client_url = function(path) { 
  return "http://www.freebase.com" + path;
};

var view_url = function(path) { 
  return "http://www.freebase.com/view" + path;
};